package com.orego.battlecrane.bc.engine.api.scenario.configuration.race

import com.orego.battlecrane.bc.engine.api.scenario.configuration.BConfiguration

/**
 * Installs configuration for player.
 */

abstract class BRaceConfiguration(protected val playerId: Long) : BConfiguration