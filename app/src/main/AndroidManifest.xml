<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.orego.battlecrane">

    <application
            android:name=".core.BApplication"
            android:allowBackup="true"
            android:icon="@mipmap/bc_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/bc_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name=".core.BActivity"
                android:screenOrientation="landscape"
                android:theme="@style/AppFullScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
